# Phase 7 Testing

Planned checks:
- `npm run lint`
- `npm test`
- Manual sanity for report generation/signing UI with CSRF token present (job/submission IDs synthetic in dev).

Executed:
- `npm run lint` (pass)
- `npm test` (pass)
