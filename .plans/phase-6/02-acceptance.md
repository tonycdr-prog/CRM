# Acceptance Criteria

- Migration adds smoke control library tables and seeds standards-aligned system types with required entities.
- Catalog endpoints list system types and required entities for a given code.
- Generator creates a template + version with required entities for a selected system type without duplicates.
- Admin UI allows selecting a system type, naming a template, and generating with clear empty/loading/error states.
- Lint and test suites pass.
